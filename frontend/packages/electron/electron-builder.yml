# electron-builder 占位配置（0.4.0 #48 打包启用；electron-shell-dev 技能模板）
# 本文件仅在 0.4.0 打包里程碑生效（ADR-019 v2），当前仅作占位。
appId: com.inkflow.app
productName: InkFlow
directories:
  output: dist
files:
  - out/**
  - ../renderer/dist/**
# 0.4.0 打包时启用：将内核可执行目录作为额外资源打进安装包
# extraResources:
#   - from: ../kernel
#     to: kernel
win:
  target:
    - nsis
    - portable
